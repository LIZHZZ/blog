# 📅 会议信息更新指南

## 📝 快速更新

编辑文件：`source/_data/conferences.yml`

## ✏️ 常见操作

### 1. 添加新会议

在 `conferences:` 列表中添加新项：

```yaml
conferences:
  # ... 现有会议 ...
  
  - name: "新会议名称"
    abbr: "缩写"
    ccf: "A"  # 或 "B" 或 "C"
    deadline: "2025-07-20"  # 格式：YYYY-MM-DD
    location: "会议地点"
    website: "https://conference-website.com/"
    priority: "high"  # high/medium/low
    note: "会议备注信息"
```

### 2. 修改会议信息

找到对应的会议项，直接修改字段值：

```yaml
  - name: "International Conference on Very Large Data Bases"
    abbr: "VLDB"
    ccf: "A"
    deadline: "2025-03-15"  # ← 修改截止日期
    location: "Barcelona, Spain"
    website: "https://vldb.org/"
    priority: "high"
    note: "数据库领域顶级会议"
```

### 3. 删除会议

直接删除整个会议项（从 `- name:` 到下一个 `- name:` 之间的所有内容）：

```yaml
conferences:
  - name: "会议1"
    # ... 其他字段 ...
  
  # 删除这个会议，直接删除下面整个块
  # - name: "要删除的会议"
  #   abbr: "XXX"
  #   ...
  
  - name: "会议2"
    # ... 其他字段 ...
```

### 4. 更新截止日期

只需修改 `deadline` 字段：

```yaml
  - name: "VLDB"
    deadline: "2025-04-01"  # 更新为新的截止日期
```

## 📋 完整示例

```yaml
conferences:
  # 示例1：完整信息
  - name: "International Conference on Very Large Data Bases"
    abbr: "VLDB"
    ccf: "A"
    deadline: "2025-03-15"
    location: "Barcelona, Spain"
    website: "https://vldb.org/"
    priority: "high"
    note: "数据库领域顶级会议"
  
  # 示例2：最小信息（只填必填项）
  - name: "ACM Conference on Example"
    deadline: "2025-08-01"
  
  # 示例3：无CCF等级
  - name: "International Workshop on Example"
    abbr: "IWE"
    deadline: "2025-09-10"
    location: "Online"
    website: "https://example.org/"
    note: "在线会议"
```

## 🔄 更新后操作

修改 `conferences.yml` 文件后，需要重新生成网站：

```bash
# 方法1：完整重新生成（推荐）
npx hexo clean
npx hexo generate
npx hexo server

# 方法2：快速生成（如果服务器正在运行）
npx hexo generate
# 然后刷新浏览器页面
```

## 📌 字段说明速查

| 字段 | 必填 | 格式 | 说明 |
|------|------|------|------|
| `name` | ✅ | 字符串 | 会议完整名称 |
| `deadline` | ✅ | YYYY-MM-DD | 截止日期 |
| `abbr` | ❌ | 字符串 | 会议缩写 |
| `ccf` | ❌ | A/B/C | CCF等级 |
| `location` | ❌ | 字符串 | 会议地点 |
| `website` | ❌ | URL | 会议官网 |
| `priority` | ❌ | high/medium/low | 优先级 |
| `note` | ❌ | 字符串 | 备注信息 |

## 💡 更新技巧

### 技巧1：批量更新日期
如果多个会议的日期都延后了，可以批量查找替换：
- 查找：`deadline: "2025-03-15"`
- 替换：`deadline: "2025-04-15"`

### 技巧2：按年份分组
可以在文件中添加注释来分组：

```yaml
conferences:
  # ===== 2025年会议 =====
  - name: "会议1"
    deadline: "2025-03-15"
  
  # ===== 2026年会议 =====
  - name: "会议2"
    deadline: "2026-01-10"
```

### 技巧3：使用模板
复制一个现有会议项，然后修改字段值，这样不容易出错。

## ⚠️ 常见错误

1. **日期格式错误**
   - ❌ 错误：`deadline: "2025/03/15"` 或 `deadline: "03-15-2025"`
   - ✅ 正确：`deadline: "2025-03-15"`

2. **YAML缩进错误**
   - ❌ 错误：使用Tab或缩进不一致
   - ✅ 正确：使用2个空格缩进

3. **缺少引号**
   - ❌ 错误：`name: International Conference`（如果包含特殊字符）
   - ✅ 正确：`name: "International Conference"`

4. **列表格式错误**
   - ❌ 错误：缺少 `-` 符号
   - ✅ 正确：每个会议项前必须有 `-`

## 🔗 参考资源

- **CCF DDL**: https://ccfddl.com/ - 中国计算机学会推荐会议截止日期
- **Conference Deadlines**: https://aideadlin.es/ - 国际会议截止日期汇总
- **会议数据文件**: `source/_data/conferences.yml`

## 📞 需要帮助？

如果更新遇到问题：
1. 检查YAML格式是否正确（可以使用在线YAML验证器）
2. 确保日期格式为 `YYYY-MM-DD`
3. 确保缩进使用空格而不是Tab
4. 重新生成网站后查看效果

---

**最后更新**: 2024-12-20

